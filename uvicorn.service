# Deployment configuration for FastAPI app using Uvicorn

[Unit]
Description=Uvicorn instance to serve FastAPI app
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/workspaces/mslearn-copilot-codespaces-python/webapp
ExecStart=/workspaces/mslearn-copilot-codespaces-python/.venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000 --proxy-headers --forwarded-allow-ips="*"
Restart=always

[Install]
WantedBy=multi-user.target
